name: E2E Playwright Tests

on:
  push:
    branches: [ main, master, fixes ]
  pull_request:
    branches: [ main, master, fixes ]

jobs:
  e2e-tests:
    name: Run Playwright E2E Tests
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: |
        npm ci
        npx playwright install chromium
        
    - name: Configure /etc/hosts for domain resolution
      run: |
        echo "Adding student12.genaicrew.com to /etc/hosts"
        echo "89.167.64.170 student12.genaicrew.com" | sudo tee -a /etc/hosts
        cat /etc/hosts | grep student12

    - name: Run E2E tests against remote server
      env:
        TEST_URL: https://student12.genaicrew.com
      run: |
        echo "Running tests against $TEST_URL"
        npx playwright test --reporter=list,html
        
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report
        path: |
          report/
          test-results/
        retention-days: 30
